# Genspark KB Extractor — Python dependencies
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# Browser automation (required)
playwright>=1.44.0

# HTTP client — used for remote config + KB API sends (required)
requests>=2.31.0

# Google Sheets export (optional — skip if you only need CSV)
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
